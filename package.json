{
  "name": "stella-trader",
  "version": "3.0.0",
  "private": true,
  "workspaces": [
    "bot",
    "dashboard"
  ],
  "scripts": {
    "bot": "npm run start --workspace=bot",
    "dev": "npm run dev --workspace=dashboard",
    "build": "npm run build --workspace=dashboard",
    "deploy": "npm run build && gh-pages -d dashboard/out"
  }
}
